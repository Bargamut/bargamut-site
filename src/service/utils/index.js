const { getPictureFileName } = require('./getPictureFileName');
const { getRandomInt } = require('./getRandomInt');
const { shuffleArray } = require('./shuffleArray');

module.exports = {
	getPictureFileName,
	getRandomInt,
	shuffleArray,
};